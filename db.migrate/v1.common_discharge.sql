CREATE TABLE `common_discharge` (
 `discharge_id` int NOT NULL AUTO_INCREMENT,
 `discharge_code` varchar(55) DEFAULT NULL,
 `discharge_value` varchar(55) DEFAULT NULL,
 `discharge_description` varchar(55) DEFAULT NULL,
 PRIMARY KEY (`discharge_id`)
 );

